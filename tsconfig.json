{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // caminho da raiz
    "paths": {
      "@/": ["./*"] // apelido/alias para chegar a raiz
    }
  }
}
